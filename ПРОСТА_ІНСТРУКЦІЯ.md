# 🚀 Проста інструкція для push

## Проблема: не можу вставити пароль в термінал

**Рішення:** Налаштувати токен в URL, щоб не вводити пароль взагалі!

---

## ✅ Крок 1: Створіть токен

1. Відкрийте: **https://github.com/settings/tokens/new?type=beta**
2. Натисніть **"Tokens (classic)"** в лівому меню
3. Натисніть **"Generate new token"** → **"Generate new token (classic)"**
4. Заповніть:
   - **Note:** `HR Bot Deploy`
   - **Expiration:** оберіть (90 days)
   - **Select scopes:** поставте ✅ **`repo`**
5. Натисніть **"Generate token"**
6. **Скопіюйте токен** (починається з `ghp_...`)

---

## ✅ Крок 2: Виконайте команди в терміналі

Відкрийте термінал і виконайте:

```bash
cd "/Users/alonalozova/Desktop/ЧАТ БОТ для HR"
```

Потім виконайте цю команду (замініть `YOUR_TOKEN` на ваш токен):

```bash
git remote set-url origin https://YOUR_TOKEN@github.com/alonalozova/HR-.git
```

**Приклад:**
Якщо ваш токен `ghp_abc123xyz456`, команда буде:
```bash
git remote set-url origin https://ghp_abc123xyz456@github.com/alonalozova/HR-.git
```

Після цього виконайте:
```bash
git push origin main
```

Тепер **не буде запитувати пароль**! ✅

---

## 🔄 Альтернатива: Використати скрипт

Якщо не хочете вводити команди вручну, можна використати скрипт:

```bash
cd "/Users/alonalozova/Desktop/ЧАТ БОТ для HR"
./SETUP_TOKEN.sh
```

Скрипт запитає токен і налаштує все автоматично.

---

## ⚠️ Важливо

- **Не надсилайте токен через чат** - це небезпечно!
- Токен як пароль - тримайте його в секреті
- Після налаштування токену в URL, вам не потрібно буде вводити пароль

---

**Готово!** Тепер push має працювати без запиту паролю! 🎉


