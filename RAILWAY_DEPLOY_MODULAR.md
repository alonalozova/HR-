# 🚀 Деплой модульної версії HR Бота в Railway

## Швидкий старт

### 1. Оновіть конфігурацію Railway

Файл `railway.json` вже налаштований для модульної версії:
```json
{
  "deploy": {
    "startCommand": "node app.js"
  }
}
```

### 2. Перевірте змінні оточення

Переконайтеся, що в Railway встановлені всі необхідні змінні:

**Обов'язкові:**
- `BOT_TOKEN` - токен Telegram бота
- `WEBHOOK_URL` - URL вашого Railway сервісу (наприклад: `https://your-app.railway.app`)
- `SPREADSHEET_ID` - ID Google таблиці
- `GOOGLE_SERVICE_ACCOUNT_EMAIL` - email сервісного акаунту Google
- `GOOGLE_PRIVATE_KEY` - приватний ключ Google (з `\n` для переносів рядків)

**Опціональні:**
- `HR_CHAT_ID` - ID чату HR для отримання заявок
- `PORT` - порт (за замовчуванням 3000, Railway встановить автоматично)

### 3. Деплой через Railway CLI

```bash
# Встановіть Railway CLI (якщо ще не встановлено)
npm i -g @railway/cli

# Увійдіть в Railway
railway login

# Ініціалізуйте проект (якщо ще не зроблено)
railway init

# Деплой
railway up
```

### 4. Деплой через GitHub

1. Закомітьте зміни:
```bash
git add .
git commit -m "Модульна версія для Railway"
git push origin main
```

2. Railway автоматично задеплоїть зміни з GitHub

### 5. Перевірка деплою

Після деплою перевірте:

1. **Health check:**
   ```bash
   curl https://your-app.railway.app/
   ```
   Має повернути: `{"status":"OK","message":"HR Bot Modular is running",...}`

2. **Webhook:**
   - Перевірте логи Railway - має бути повідомлення про успішне встановлення webhook
   - Відправте `/start` боту в Telegram

## Переваги модульної версії на Railway

✅ **Швидший старт** - lazy loading модулів  
✅ **Менше пам'яті** - завантажуються тільки потрібні модулі  
✅ **Краща продуктивність** - оптимізований кеш та обробка  
✅ **Легше дебажити** - структуровані логи по модулях

## Відкат до оригінальної версії

Якщо потрібно повернутися до оригінальної версії:

1. Змініть `railway.json`:
```json
{
  "deploy": {
    "startCommand": "node HR_Bot_Complete_Ultimate.js"
  }
}
```

2. Або встановіть змінну оточення в Railway:
```
RAILWAY_START_COMMAND=node HR_Bot_Complete_Ultimate.js
```

## Моніторинг

Перевіряйте логи в Railway Dashboard:
- Health check запити
- Webhook запити
- Помилки та попередження
- Статистика кешу (кожні 10 хвилин)

## Troubleshooting

### Бот не відповідає
1. Перевірте `BOT_TOKEN` в змінних оточення
2. Перевірте `WEBHOOK_URL` - має бути правильний URL вашого Railway сервісу
3. Перевірте логи Railway на наявність помилок

### Google Sheets не працює
1. Перевірте `SPREADSHEET_ID`, `GOOGLE_SERVICE_ACCOUNT_EMAIL`, `GOOGLE_PRIVATE_KEY`
2. Переконайтеся що приватний ключ має `\n` для переносів рядків
3. Перевірте права доступу сервісного акаунту до таблиці

### Помилки завантаження модулів
1. Перевірте що всі файли закомічені в Git
2. Перевірте структуру папок в Railway
3. Перевірте логи на помилки require()

## Підтримка

Модульна версія повністю сумісна з оригінальною. Всі функції працюють так само.

