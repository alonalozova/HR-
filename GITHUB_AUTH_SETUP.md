# 🔐 Налаштування автентифікації GitHub

## 📝 Інструкція

### Крок 1: Створіть Personal Access Token

1. Відкрийте: https://github.com/settings/tokens
2. Натисніть **"Generate new token"** → **"Generate new token (classic)"**
3. Назва: `HR Bot Deploy`
4. Термін дії: оберіть (90 днів або без терміну)
5. Права (scopes):
   - ✅ **`repo`** - повний доступ до репозиторіїв
   - ✅ **`workflow`** - для GitHub Actions (якщо використовуєте)
6. Натисніть **"Generate token"**
7. **ВАЖЛИВО:** Скопіюйте токен зараз! Він показується тільки один раз.

---

## 🚀 Варіант A: Використання токену в URL (для push)

```bash
cd "/Users/alonalozova/Desktop/ЧАТ БОТ для HR"

# Замініть YOUR_TOKEN на ваш токен
git remote set-url origin https://YOUR_TOKEN@github.com/alonalozova/HR-.git

# Тепер можна робити push
git push origin main
```

**⚠️ Увага:** Токен буде збережений у конфігурації git. Для безпеки краще використовувати SSH ключі.

---

## 🔑 Варіант B: SSH ключі (рекомендовано для безпеки)

### 1. Створіть SSH ключ (якщо ще немає):

```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
```

Натисніть Enter для збереження за замовчуванням (`~/.ssh/id_ed25519`).

### 2. Додайте SSH ключ до ssh-agent:

```bash
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
```

### 3. Скопіюйте публічний ключ:

```bash
cat ~/.ssh/id_ed25519.pub
```

### 4. Додайте ключ на GitHub:

1. Відкрийте: https://github.com/settings/keys
2. Натисніть **"New SSH key"**
3. Назва: `MacBook Air`
4. Вставте скопійований публічний ключ
5. Натисніть **"Add SSH key"**

### 5. Змініть remote на SSH:

```bash
cd "/Users/alonalozova/Desktop/ЧАТ БОТ для HR"
git remote set-url origin git@github.com:alonalozova/HR-.git

# Тестування підключення
ssh -T git@github.com

# Якщо все ОК, можна робити push
git push origin main
```

---

## 🔄 Варіант C: GitHub CLI (gh)

### 1. Встановіть GitHub CLI:

```bash
brew install gh
```

### 2. Авторизуйтесь:

```bash
gh auth login
```

Слідуйте інструкціям на екрані.

### 3. Тепер можна робити push:

```bash
git push origin main
```

---

## ✅ Перевірка після налаштування

```bash
# Перевірка remote URL
git remote -v

# Тест push (якщо є зміни)
git push origin main
```

---

## 🆘 Якщо щось не працює

1. **Помилка "Permission denied"**:
   - Перевірте чи правильний токен/ключ
   - Перевірте чи маєте права доступу до репозиторію

2. **Помилка "Repository not found"**:
   - Перевірте назву репозиторію
   - Переконайтеся що репозиторій існує на GitHub

3. **Помилка "Authentication failed"**:
   - Перевірте чи токен не прострочений
   - Перевірте чи правильно введено токен

---

## 🔒 Безпека

- **Ніколи не комітьте токени в код!**
- Токени мають бути тільки в environment variables або в `.gitignore`
- Використовуйте SSH ключі для постійної роботи
- Регулярно оновлюйте токени

---

**Рекомендація:** Використовуйте SSH ключі (Варіант B) для безпеки та зручності.




