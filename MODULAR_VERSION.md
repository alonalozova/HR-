# 🚀 Модульна версія HR Бота

## Опис

Модульна версія бота розділена на окремі модулі для покращення продуктивності та підтримуваності коду.

## Переваги модульної версії

✅ **Швидше завантаження** - модулі завантажуються тільки коли потрібні (lazy loading)  
✅ **Краща організація** - код розділений на логічні модулі  
✅ **Легше підтримувати** - кожен модуль відповідає за свою функціональність  
✅ **Краща продуктивність** - оптимізований кеш та обробка помилок  
✅ **Масштабованість** - легко додавати нові функції

## Структура модулів

```
├── app.js                    # Головний файл (точка входу)
├── config/                   # Конфігурація
│   └── index.js             # Налаштування та константи
├── utils/                    # Утиліти
│   ├── cache.js             # Кеш з TTL
│   ├── errors.js             # Класи помилок
│   ├── logger.js              # Логер
│   ├── retry.js               # Retry логіка
│   └── validation.js          # Валідація
├── services/                 # Сервіси
│   ├── sheets.service.js     # Google Sheets
│   ├── telegram.service.js    # Telegram Bot API
│   └── user.service.js       # Робота з користувачами
└── handlers/                 # Обробники
    ├── message.handler.js     # Обробка повідомлень
    └── callback.handler.js    # Обробка callback'ів
```

## Запуск

### Модульна версія (рекомендовано)
```bash
npm run start:modular
# або
npm run start:fast
# або
node app.js
```

### Оригінальна версія
```bash
npm start
# або
node HR_Bot_Complete_Ultimate.js
```

## Як це працює

1. **app.js** - ініціалізує сервіси та налаштовує Express сервер
2. **Webhook endpoint** - отримує оновлення від Telegram
3. **Lazy loading** - handlers завантажуються тільки коли потрібні
4. **Сервіси** - використовують кеш та retry логіку для швидкості
5. **Handlers** - обробляють повідомлення та callback'и

## Оптимізації

- ⚡ **Lazy loading** - модулі завантажуються тільки коли потрібні
- 💾 **Кешування** - користувачі та запити кешуються з TTL
- 🔄 **Retry логіка** - автоматичні повторні спроби при помилках
- 📊 **Моніторинг** - відстеження продуктивності та помилок
- 🛡️ **Захист від дублювання** - перевірка update_id

## Міграція

Модульна версія використовує той самий код, що й оригінальна версія, але з кращою організацією. Всі функції доступні через експорт з `HR_Bot_Complete_Ultimate.js`.

## Налаштування

Всі налаштування в файлі `.env` та `config/index.js`.

## Підтримка

Модульна версія повністю сумісна з оригінальною версією. Можна використовувати будь-яку версію.

